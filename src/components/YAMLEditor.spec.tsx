//import * as React from 'react';
//import { render } from '@testing-library/react';
//import { YAMLEditor } from './YAMLEditor';

const handleChangesMock = jest.fn();

afterEach(() => {
  handleChangesMock.mockReset();
});

/*
const renderMocked = () => {
  return render(
    <YAMLEditor handleChanges={handleChangesMock}/>
  );
};

 */

it('', () => {});

export {}
