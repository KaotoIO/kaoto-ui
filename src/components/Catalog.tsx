// import * as React from 'react';
import { IStepProps } from '../types';

interface ICatalog {
  end?: IStepProps[],
  middle?: IStepProps[],
  start?: IStepProps[]
}

const Catalog = ({ end, middle, start }: ICatalog) => {
  //
  return (<></>);
};

export { Catalog };
