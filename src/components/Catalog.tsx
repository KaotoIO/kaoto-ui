// import * as React from 'react';

interface ICatalog {
  actions?: [],
  sinks?: [],
  sources?: []
}

const Catalog = ({ actions, sinks, sources }: ICatalog) => {
  //
  return (<></>);
};

export { Catalog };
