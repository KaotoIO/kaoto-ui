describe.skip('step extensions', () => {
  beforeEach(() => {
    cy.visit('http://localhost:1337');
  });

  it('loads the step extension', () => {
    //cy.get('[data-testid]="step-extension"').should('exist');
  });
});
